# Default deny all ingress traffic in default namespace
# This should break the eventing demo (dispatcher can't reach the app)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Ingress
